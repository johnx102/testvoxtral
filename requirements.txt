# ====================================================================
# IMPORTANT: torch, torchaudio, torchvision sont installés dans le Dockerfile
# Ne PAS les inclure ici pour éviter les conflits de version CUDA
# ====================================================================

# Transformers - version stable avec support Voxtral
# Le support Voxtral est inclus depuis transformers 4.47+
transformers>=4.47.0

# HuggingFace et ML core
accelerate>=0.33.0
huggingface_hub>=0.26.0
scipy>=1.10.1
sentencepiece>=0.2.0
safetensors>=0.4.0

# Mistral/Voxtral tokenizer dependency (OBLIGATOIRE pour Voxtral)
mistral-common>=1.8.6

# Audio processing
soundfile>=0.12.1
librosa>=0.10.1
numpy>=1.24.3,<2.0.0
pydub>=0.25.1

# PyAnnote pour diarization
pyannote.audio>=3.1.1
speechbrain>=0.5.16

# API et utils
requests>=2.31.0
runpod>=1.6.2
psutil>=5.9.0
